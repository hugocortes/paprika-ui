import React from 'react';
import { RouteComponentProps } from 'react-router';

import { Profile } from './../../../components';

export const ProfileRoute = (props: RouteComponentProps) => {
  return (
    <Profile />
  )
}
